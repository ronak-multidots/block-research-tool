// Dynamic block: markup is generated by render.php from saved attributes
// and the serialized inner blocks, so `save` only needs to preserve the
// InnerBlocks content in the post's stored HTML comment delimiters.
import { InnerBlocks } from '@wordpress/block-editor';

export default function save() {
	return <InnerBlocks.Content />;
}
